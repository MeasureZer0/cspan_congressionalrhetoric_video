name: Ruff

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: astral-sh/ruff-action@v3.5.1
              with:
                  args: "format --check --diff"
